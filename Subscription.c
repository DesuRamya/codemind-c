#include<stdio.h>
int main()
{
    int i,t,x;
    scanf("%d",&t);
    while(t--)
    {
        scanf("%d",&x);
        if(x>30)
        {
            printf("YES");
        }
        else{
            printf("NO");
        }
        printf("
");
    }
}